{
  "name": "vkosapp",
  "version": "1.5.0",
  "private": true,
  "description": "VKOS Hub. Cockpit web local multi-IA, orquestrando o VKOS pelo Cerebro.",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n servidor,web -c green,cyan \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
