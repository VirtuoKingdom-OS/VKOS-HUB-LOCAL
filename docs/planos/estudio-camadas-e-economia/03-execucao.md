# Camadas e economia: plano de execução

Regras de sempre (colar em TODO prompt de agente): português brasileiro, sem travessão "—" nem "·", frase curta, cores da UI só por token (base `global.css`, camada final `visual-hub.css`), tudo funciona nos 3 temas (Escuro, Dark VKOS, Claro), motion sutil com prefers-reduced-motion, NUNCA commit nem push. Raiz: `e:\@OJESSEGOMES - OS CREATOR\VKOS\VKOS-APP\VKOSAPPv2`. Servidores possivelmente no ar (4600, 5173): não derrubar. Typecheck a partir de `app/`: `npm run checar -w server` e `-w web`. Testes: `npm run testar -w server` e `-w web`. Cada dono lê `LEIA-ME.md`, `01-visao.md` e `02-arquitetura.md` inteiros antes de editar, e só toca os arquivos da sua fronteira. Dados reais do Jesse (OJESSEGOMES, Estúdio Aura) são sagrados: teste só em workspace de teste; a peça real "2026-07-20-7-ferramentas-do-google" não pode ser editada, só uma CÓPIA do template dela em workspace de teste.

## Fase 1 (donos A e C em paralelo)

### Dono A (Opus): hit-test, camadas e imagem própria no carrossel

Fronteira: `web/src/componentes/editor/motor.ts`, `editor/nucleo.ts`, `editor/PainelCamadas.tsx` (novo), `editor/EditorCarrossel.tsx`, `editor/ControlesImagem.tsx`, `studio/TelaStudio.tsx`, `studio/PainelPropriedades.tsx`, estilos do editor/studio.

Prompt pronto:

> Você é o Dono A da rodada Camadas e Economia do VKOS Hub. Leia docs/planos/estudio-camadas-e-economia/ (LEIA-ME, 01-visao, 02-arquitetura) inteiros e siga as regras de sempre. Sua missão, na ordem: (1) E1 do 02: troque o hit-test do clique do carrossel (`motor.ts:657-674`, hoje `e.target` puro) pela descida geométrica `alvoNoPonto` do núcleo (`nucleo.ts:89-120`), parametrizando o filtro de camada decorativa pra não valer no carrossel, e implemente o clique repetido que alterna entre os elementos empilhados sob o ponteiro; o duplo clique continua refinando com `alvoEdicao`; confirme que um `<span>` com `pointer-events:none` (caso das aspas dos templates) vira selecionável e editável SEM mudar template nenhum. (2) E2: semeie `data-vk` em todo elemento do slide na instrumentação, preserve `data-vk` na serialização, implemente `listarCamadas` (dois níveis, ordenados por empilhamento efetivo) e `moverCamada(id, direcao)` com a regra DOM+z-index do 02, e faça o desfazer re-selecionar pelo `data-vk`. (3) E2.5: crie `PainelCamadas.tsx` compartilhável (props: itens, selecionadoId, aoSelecionar, aoMover), renderize no Studio e no overlay, linhas com nome amigável, setas de subir/descer, estilos por token nos 3 temas; publique a assinatura de props no seu relatório, o Dono B vai consumir. (4) E3: `inserirImagemLivre` no motor (img absoluta, 40% de largura, centralizada, z-index acima do conteúdo, selecionada ao nascer, arrastável pelo arrasto existente) e botão "Adicionar imagem" com as duas origens que `ControlesImagem` já tem, mais campo de largura no painel de propriedades. Valide manualmente com Playwright em http://localhost:5173 num workspace de teste, criando uma peça de carrossel copiando um template com aspas `pointer-events:none` (copie a anatomia de `vkos2/templates/carrossel/modelo-declaracao.html` pra peça de teste). Ao final: `npm run checar -w web` limpo, `npm run testar -w web` verde, relatório com o que mudou, a assinatura do PainelCamadas, screenshots e ressalvas honestas.

### Dono C (Opus): modelos baratos e interruptor Aprimorar com IA

Fronteira: `web/src/componentes/criacao/**`, `server/src/provedores/contrato.ts`, `claude.ts`, `codex.ts` (só o campo `economico`), `web/src/estado/provedores.ts` (tipo), `studio/PainelAjusteCarrossel.tsx`, `site/TelaSite.tsx` (SOMENTE a inicialização do modelo do painel Ajustar com IA), testes.

Prompt pronto:

> Você é o Dono C da rodada Camadas e Economia do VKOS Hub. Leia docs/planos/estudio-camadas-e-economia/ (LEIA-ME, 01-visao, 02-arquitetura) inteiros e siga as regras de sempre. Sua missão: (1) C1 do 02: campo `economico?: boolean` no contrato de modelo dos provedores, marcado no `haiku` (Claude) e no `gpt-5.4-mini` (Codex); o painel Ajustar com IA do carrossel e do site inicializa o modelo no econômico do provedor ativo com a dica curta do 02; a Revisão de design continua inicializando no padrão. (2) C2: campo `aprimorarComIA` (padrão true) nos dados dos dois wizards, interruptor na etapa de instruções finais com subtexto honesto, seletor de modelo da etapa 0 desabilitado com nota quando desligado, aviso inline quando desligado com detalhes vazio. (3) O payload força o modelo econômico da tarefa quando desligado (carrossel: haiku/gpt-5.4-mini; site: sonnet/gpt-5.6-terra) e anexa o bloco de montagem: `blocoMontagemEconomica()` em `prompt.ts` (siga o template sem alterar anatomia, conteúdo vem das instruções finais e do Cérebro, não invente direção de arte, não adicione elementos) e a variante do site em `promptSite.ts` (substitui SÓ o Bloco 1 pelo bloco enxuto com estilo fixo da biblioteca; escolha como padrão o estilo mais neutro e legível do índice `templates/design/estilos/indice.md` e cite qual escolheu no relatório; Blocos 2 e 3 intactos). PROIBIDO alterar qualquer linha existente dos prompts do modo ligado: o modo econômico só ANEXA ou SUBSTITUI blocos inteiros pela chave nova. (4) Testes de `prompt.ts`/`promptSite.ts` cobrindo: ligado gera prompt idêntico ao atual (snapshot), desligado contém o bloco de montagem e não contém o Bloco 1 de design (site). Ao final: `npm run checar -w web` e `-w server` limpos, testes verdes, relatório com prompts de exemplo dos dois modos e ressalvas.

## Fase 2 (dono B, depois do relatório de A)

### Dono B (Opus): paridade no editor de site

Fronteira: `editor/motorSite.ts`, `site/PainelSite.tsx`, `site/TelaSite.tsx` (só o modo editar), estilos do site.

Prompt pronto:

> Você é o Dono B da rodada Camadas e Economia do VKOS Hub. Leia docs/planos/estudio-camadas-e-economia/ (LEIA-ME, 01-visao, 02-arquitetura) inteiros, o relatório do Dono A (assinatura do PainelCamadas) e siga as regras de sempre. Sua missão: (1) E4 do 02: dentro da seção selecionada, `listarCamadas` da seção (elementos de primeiro nível) e `moverCamada` por ordem no DOM, renderizando o `PainelCamadas` compartilhado no modo editar do site junto da lista de seções que já existe; (2) E3 do site: `inserirImagemLivre` inserindo `<img>` de bloco no fim da seção selecionada (largura 100%, max-width ajustável), botão "Adicionar imagem" no PainelSite com as duas origens; (3) confirme que serializar preserva tudo e que o laço de auditoria de publicação aceita a imagem (caminho relativo `img/`). Valide com Playwright num site de workspace de teste. NÃO toque em `nucleo.ts` nem `PainelCamadas.tsx`: se faltar algo neles, reporte pro executor arbitrar. Ao final: `npm run checar -w web` limpo, testes verdes, relatório e ressalvas.

## Fase 3: QA de gesto real (Opus)

Prompt pronto:

> Você é o QA da rodada Camadas e Economia do VKOS Hub. Leia docs/planos/estudio-camadas-e-economia/ e os relatórios dos donos. Playwright em http://localhost:5173, viewport 1440x900, screenshots. REGRA SAGRADA: nada nos workspaces OJESSEGOMES e Estúdio Aura; crie um workspace de teste pela UI e apague no fim. Roteiro: (1) conformidade: typechecks e testes verdes, grep de travessão e ponto centrado nos textos novos, nenhuma cor hardcoded nos arquivos novos. (2) Editor carrossel: crie uma peça de teste com a anatomia do modelo-declaracao (aspas `pointer-events:none`, num-bg, imagem de página em z-index baixo); clique nas aspas e confirme seleção e edição de texto; clique na imagem de página e confirme seleção; clique repetido alterna empilhados; painel de camadas lista, seleciona e reordena; salvar, reabrir e conferir que a ordem persistiu; desfazer restaura. (3) Imagem própria: adicione imagem do computador no carrossel, arraste, mude largura, suba e desça de camada, salve e confira o HTML gravado; mesmo teste no site (imagem de bloco na seção). (4) Economia: no wizard de carrossel, desligue o Aprimorar com IA, confirme o aviso com detalhes vazio, escreva um roteiro curto nas instruções, gere, e confirme no custos.json do workspace de teste que a sessão rodou no modelo econômico; compare visualmente com uma geração ligada do mesmo tema (2 gerações no total, workspace de teste, tema curto). Se o executor não autorizar gerações reais, PARE essa parte e reporte pendente, sem inventar. (5) Site: desligado, confirme que o prompt enviado (olhe a transcricao da sessão) não contém o bloco de design e contém o estilo fixo, e que o laço de conformidade rodou. (6) 3 temas no painel de camadas e no interruptor, screenshots. (7) Apague o workspace de teste. Relatório: matriz passo a passo com veredito, bugs com severidade e arquivo:linha. Não conserte nada, só reporte.

## Checklist de fechamento (executor)

1. Bugs de QA de severidade média pra cima corrigidos pelo dono da fronteira certa e revalidados.
2. `npm run build -w web` a partir de `app/` e conferir o hash na 4600.
3. `app/CONTRATO.md`: painel de camadas e ids `data-vk` no carrossel, inserir imagem própria nos dois editores, campo `economico` no contrato de provedores, interruptor `aprimorarComIA` nos payloads dos wizards e a regra de modelo forçado no modo desligado.
4. `docs/contexto/roadmap.md` e `docs/contexto/arquitetura.md` na linha certa; `interno/mapa-sistema.json` (nó do Studio e do Site Guiado: camadas e modo econômico; nó de sessões: modelos por tarefa).
5. `docs/decisoes/2026-07-20-camadas-e-modo-economico.md`: contexto (bugs do caso real, custo por modelo), decisão (hit-test geométrico, data-vk no carrossel, painel compartilhado, econômico por tarefa, interruptor com montagem), por quê (régua de preços, qualidade protegida pelo desvio explícito e pelo laço).
6. Avisar o Jesse com o resumo honesto, incluindo a comparação visual ligado vs desligado do QA e o custo real registrado das gerações de teste.
7. Perguntar ao Jesse se pode apagar a pasta `docs/planos/estudio-camadas-e-economia/`.
