# Leads do Google Maps: plano de execução

Regras de sempre (colar em TODO prompt de agente): português brasileiro, sem travessão "—" nem "·", frase curta, cores da UI só por token (base `global.css`, camada final `visual-hub.css`), tudo funciona nos 3 temas (Escuro, Dark VKOS, Claro), motion sutil com prefers-reduced-motion, NUNCA commit nem push. Raiz: `<raiz do repositorio>`. Servidores possivelmente no ar (4600, 5173): não derrubar. Typecheck a partir de `app/`: `npm run checar -w server` e `-w web`. Testes: `npm run testar -w server` e `-w web`. Cada dono lê `LEIA-ME.md`, `01-visao.md` e `02-arquitetura.md` inteiros antes de editar, e só toca os arquivos da sua fronteira. Dados reais do Jesse são sagrados: nada de importar lead nos workspaces OJESSEGOMES e Estúdio Aura. Nenhuma sessão de IA real (a feature é determinística).

## Fase 1 (donos A e B em paralelo)

### Dono A (Opus): conexão, busca e importação

Fronteira: `conexoes/catalogo.ts` (só a entrada nova), `server/src/leads/**` (novo), `conexoes/rotas.ts` (só a linha do teste), `index.ts` (só o registro), testes do server.

Prompt pronto:

> Você é o Dono A da rodada Leads do Google Maps do VKOS Hub. Leia docs/planos/leads-google-maps/ (LEIA-ME, 01-visao, 02-arquitetura) inteiros e siga as regras de sempre. Sua missão é o backend inteiro da peça: (1) entrada `apify` no catálogo de Conexões conforme o 02, SEM `montarServidor` (é REST puro, não entra em MCP), conferindo que `montarConfigMcp` continua ignorando ela; (2) módulo novo `server/src/leads/apify.ts`: consulte a documentação pública atual da Apify ANTES de escrever (o endpoint `run-sync-get-dataset-items`, seu teto de tempo e comportamento no estouro, e os campos de entrada e saída do Actor `compass/crawler-google-places`), porque eu não fixei os nomes exatos no plano de propósito; caminho preferido é `run-sync-get-dataset-items` numa chamada só; se o teto de tempo dele for curto demais pra busca real, implemente o fallback run + polling do status + fetch do dataset TODO dentro do server, numa única requisição do frontend; toda execução do Actor manda limite explícito de resultados (constante LIMITE_RESULTADOS = 40) e idioma pt-BR; leia o token salvo no padrão de `publicacao/github.ts:20-27`; normalize a resposta pro tipo `LeadEncontrado` (nome, endereco, telefone, site, email quando o Actor devolver, categoria, nota, totalAvaliacoes, placeId); `ErroLeads` com 400 sem conexão e 502 com falha do provedor, mensagem legível, e crédito esgotado da Apify vira mensagem clara em vez de 502 genérico; exporte a função pura de normalização de telefone (só dígitos) porque ela é usada nos dois lados da duplicata; (3) `server/src/leads/rotas.ts` com `POST /api/leads/buscar` (busca, cruza com os contatos do CRM por telefone normalizado, marca `jaExisteNoCrm`, NÃO grava nada, timeout da rota de 3 minutos) e `POST /api/leads/importar` (reconfere duplicata, chama `criarContato` do CRM sem modificar `crm/estado.ts`, com `origem: "google-maps:<placeId>"` e `tags: ["google-maps"]`, devolve `{ importados, duplicados, contatos }`), registrado em `index.ts`; (4) `apify` no teste de conexão de `conexoes/rotas.ts:114-116`, validando com `GET /v2/users/me`, sem disparar Actor nenhum; (5) testes: normalização de telefone com formatos brasileiros variados (parênteses, espaço, hífen, +55, nono dígito), duplicata detectada na busca e na importação, erro sem conexão configurada, importar a mesma lista duas vezes não duplica, normalização da resposta do Actor a partir de um fixture com o formato real documentado. Ao final: `npm run checar -w server` limpo, `npm run testar -w server` verde, relatório com o contrato EXATO das duas rotas (método, corpo, resposta, é o contrato do Dono B), qual caminho da Apify você usou (síncrono ou polling) e por quê, o custo por resultado que descobriu na documentação, e ressalvas honestas.

### Dono B (Opus): aba de busca no CRM

Fronteira: `app/web/src/componentes/crm/**`, `app/web/src/api/**`, `app/web/src/estilos/crm.css`.

Prompt pronto:

> Você é o Dono B da rodada Leads do Google Maps do VKOS Hub. Leia docs/planos/leads-google-maps/ (LEIA-ME, 01-visao, 02-arquitetura) inteiros e siga as regras de sempre. Leia `TelaCrm.tsx` inteiro antes de mexer: a mecânica de abas (com `role="tablist"`, `role="tab"` e `aria-selected`) é recente e deve ser PRESERVADA, você só acrescenta uma aba. Sua missão: (1) aba nova "Buscar leads" ao lado de Hoje, Quadro e Contatos; (2) componente `BuscaLeads.tsx`: campo de termo com exemplo no placeholder ("padaria em Belo Horizonte"), busca ao enviar (Enter e botão), estado de carregando honesto e DEMORADO (a busca real leva de 20 segundos a 2 minutos porque um robô visita o Google Maps: mostre mensagem explicando isso e uma indicação viva de progresso, não um spinner mudo; desabilite o botão enquanto roda pra não disparar busca dupla), erro inline legível quando a conexão não existe (com atalho pra tela de Conexões, no padrão que a TelaSite usa quando falta conexão de publicação); (3) resultados em cartões com nome, endereço, telefone, site, categoria e nota; cartão de quem já está no CRM aparece com selo "já no CRM" e checkbox desabilitado; checkbox por cartão mais uma ação "marcar todos os novos"; rodapé fixo com "Importar N selecionados"; depois de importar, resumo honesto (quantos entraram, quantos pulados por duplicata) e a lista atualiza os selos; (4) bloco de aviso fixo no topo da aba, texto curto: dado vem de fonte pública de terceiro, usar com responsabilidade, evitar disparo em massa sem critério; (5) funções de API client `buscarLeads(termo)` e `importarLeads(leads)` conforme o contrato da peça 4 do 02-arquitetura, com timeout do fetch da busca de 3 minutos (se o Dono A ainda não tiver publicado o contrato final quando você chegar aqui, tipe pelo 02 e ajuste no fim); (6) CSS em `crm.css` com tokens, 3 temas, motion sutil, estado vazio gentil antes da primeira busca. Ao final: `npm run checar -w web` limpo, `npm run testar -w web` verde, relatório com o que mudou e ressalvas.

## Fase 2: QA de gesto real (Opus)

Prompt pronto:

> Você é o QA da rodada Leads do Google Maps do VKOS Hub. Leia docs/planos/leads-google-maps/ e os relatórios dos donos. Playwright em http://localhost:5173, viewport 1440x900, screenshots. NENHUMA sessão de IA real. REGRA SAGRADA: não importe lead nenhum nos workspaces OJESSEGOMES nem Estúdio Aura; crie um workspace de teste pela UI pra tudo e apague no fim. Roteiro: (1) conformidade: typechecks e testes verdes nos dois workspaces, grep de travessão e ponto centrado nos textos novos, nenhuma cor hardcoded nos arquivos novos. (2) Sem conexão configurada: a aba Buscar leads mostra o erro legível com atalho pra Conexões, e NÃO quebra a tela. (3) Conexão: o card da Apify aparece na tela de Conexões com o campo de token; salve um token INVÁLIDO e confirme que o teste de conexão falha com mensagem honesta; confirme que a resposta do GET de conexões traz o token MASCARADO (olhe a resposta da rede, não só a tela) e que nada de token aparece no log do servidor. (4) A PROVA REAL: peça ao executor o token real da Apify do Jesse (ele decide se fornece; se não fornecer, PARE aqui e reporte que a prova real ficou pendente, sem inventar). Com o token: busque um termo real de baixa contagem, aguarde a execução do Actor terminar (pode levar 2 minutos, o estado de carregando deve se manter honesto o tempo todo), confirme que os resultados aparecem com os campos do contrato, importe 2 ou 3, e confira no CRM do workspace de teste que os contatos entraram com a tag `google-maps` e o campo origem preenchido. (5) Duplicata: rode a MESMA busca de novo e confirme que os já importados aparecem com selo "já no CRM" e checkbox desabilitado; tente importar a mesma lista pela API direto e confirme que a resposta conta como duplicado em vez de criar de novo. (6) Integração viva: confirme que a criação do lead emitiu `crm:contato-criado` (olhe o `eventos.jsonl` do workspace de teste) e que o CRM continua funcionando normal nas outras 3 abas com os leads importados dentro. (7) 3 temas na aba nova, screenshots. (8) Resíduos: apague o workspace de teste e confirme no disco. Relatório: matriz passo a passo com veredito, bugs com severidade e arquivo:linha, screenshots. Não conserte nada, só reporte.

## Checklist de fechamento (executor)

1. Typechecks e testes verdes nos dois workspaces; bugs de QA de severidade média pra cima corrigidos pelo dono da fronteira certa e revalidados.
2. `npm run build -w web` a partir de `app/` e conferir o hash novo na 4600.
3. Atualizar `app/CONTRATO.md`: a entrada nova no catálogo de Conexões (e a contagem na linha 456, que hoje diz "GitHub, Netlify, Notion e Google Calendar"), o módulo `leads/`, as duas rotas com corpo e resposta, a regra de duplicata por telefone normalizado, e a convenção de `origem: "google-maps:<placeId>"` mais tag automática.
4. Atualizar `docs/contexto/arquitetura.md` com uma linha da fonte de leads, e `docs/contexto/roadmap.md` na fase do CRM.
5. Atualizar `interno/mapa-sistema.json`: o nó do CRM menciona a entrada de leads; considerar se o nó de Conexões precisa de ajuste.
6. Registrar `docs/decisoes/2026-07-18-leads-google-maps.md`: contexto (as quatro alternativas avaliadas com a pegadinha dos Termos do Google e a LGPD), decisão (Apify como provedor, Actor `compass/crawler-google-places` de partida, importação sempre confirmada pelo usuário, origem e tag rastreáveis, duplicata por telefone, limite explícito de resultados por busca), por quê (ecossistema aberto de Actors: fonte futura é outro Actor, não outro provedor; risco contratual de scraping fica com a Apify).
7. Avisar o Jesse com o resumo honesto, incluindo o custo real por resultado que o Dono A descobriu na documentação e qual caminho da API foi usado (síncrono ou polling).
8. Perguntar ao Jesse se pode apagar a pasta `docs/planos/leads-google-maps/`.
