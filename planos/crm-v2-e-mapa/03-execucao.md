# CRM v2 e Mapa do sistema: plano de execução

Regras de sempre (colar em TODO prompt de agente): português brasileiro, sem travessão "—" nem "·", frase curta, cores só por tokens de `app/web/src/estilos/global.css`, tudo funciona nos 3 temas (Escuro, Dark VKOS, Claro), motion sutil com prefers-reduced-motion, NUNCA commit nem push. Raiz: `e:\@OJESSEGOMES - OS CREATOR\VKOS\VKOS-APP\VKOSAPPv2`. Servidores possivelmente no ar (4600 backend, 5173 Vite): não derrubar. Typecheck a partir de `app/`: `npm run checar -w web` e `npm run checar -w server`. Testes: `npm run testar -w server`. Cada dono só toca os arquivos da sua fronteira (seção final do 02-arquitetura.md). REGRA JURÍDICA: nenhum dono abre o zip do Twenty; os conceitos já estão destilados no 01-visao.md; o código dele é AGPL e nunca entra.

REGRA DE OURO DA RODADA: os dados atuais do CRM do Jesse são sagrados. Antes de qualquer edição no server, o dono A copia os `crm.json` existentes de `app/dados/workspaces/*/` pra um backup no scratchpad da sessão e informa os caminhos no relatório. A migração é idempotente e testada.

## Fase 1 (paralela: donos A e C)

### Dono A (Opus): modelo v2, migração, rotas, eventos e o resumo pra IA

Arquivos: `app/server/src/crm/estado.ts`, `crm/rotas.ts`, `crm/resumo.ts` (novo), `app/server/src/sessoes/rotas.ts` (só a detecção do CRM no prompt), `app/server/src/sessoes/gerenciador.ts` (só o instrucoesExtras e a persistência do contexto), `app/server/src/tipos.ts` (campo na Sessao se precisar), `app/server/src/automacoes/executor.ts` (só o fallback de valorEstimado), testes do server.

Prompt pronto:

> Você é o Dono A da rodada CRM v2 e Mapa do VKOS Hub. Leia primeiro os três arquivos de planos/crm-v2-e-mapa/ (01-visao, 02-arquitetura, 03-execucao) e siga as regras de sempre e a regra de ouro (backup dos crm.json antes de tudo). Sua missão é a peça 1 da arquitetura, completa: (1) modelo v2 em crm/estado.ts conforme o 02 (Interacao, Tarefa, Contato sem colunaId/valorEstimado/notas, Negocio, EstadoCrm versao 2), com saneamento defensivo estendido; (2) migração idempotente na leitura: crm.json v1 vira v2 na hora sem perder NADA (cada contato v1 vira ficha + negócio com título igual ao nome, notas viram interações tipo nota preservando data e ordem, coluna e valor vão pro negócio, proximoContato fica na ficha), persiste como v2 na primeira gravação, arquivo v2 passa direto; (3) rotas conforme o 02: contatos como hoje (menos colunaId/valor), interações (com alias da rota antiga de notas criando interação tipo nota), tarefas, negócios com mover, colunas intocadas, erros no padrão ErroCrm; (4) eventos com compatibilidade total: os 4 eventos de contato mantêm o formato; mover negócio emite crm:contato-movido com { contato, negocio, colunaDe, colunaPara, nomeColunaDe, nomeColunaPara }; interação registrada emite crm:interacao-registrada { contato, interacao }; em automacoes/executor.ts, variaveisDoEvento passa a ler valorEstimado de dados.negocio?.valorEstimado ?? dados.contato?.valorEstimado (retrocompat com eventos históricos do ensaio); NÃO toque em calendario/sincronizacao.ts (proximoContato continua na ficha e os eventos de contato continuam iguais, nada muda lá); (5) crm/resumo.ts novo com montarResumoCrm() conforme o 02 (funil com contagem e valor por coluna, follow-ups atrasados e próximos 7 dias, top 8 tags, esquecidos 30+ dias, as 15 interações mais recentes truncadas a 200 caracteres, NUNCA telefone nem email no texto, teto de 8 KB, null quando vazio); (6) a injeção: em sessoes/rotas.ts POST /sessoes, se /\bcrm\b/i casar no prompt e montarResumoCrm() devolver texto, passe o resumo ao gerenciador; no gerenciador, persista na sessão (padrão do modoEnxuto) e monte instrucoesExtras como concatenação do que existir (regra enxuta e/ou bloco do CRM no formato exato do 02, com a REGRA DURA anti-vazamento), retomada repete; (7) testes do server: migração v1 pra v2 completa (fixture com contato cheio), idempotência (ler duas vezes não duplica), resumo sem telefone/email mesmo com esses campos preenchidos, detecção do regex (casa "CRM", "crm", não casa "crmx"). Ao final: npm run checar -w server limpo, npm run testar -w server verde, relatório com o contrato EXATO das rotas novas (método, caminho, corpo, resposta, é o contrato do Dono B), os caminhos dos backups, e ressalvas honestas.

### Dono C (Opus): Mapa do sistema

Arquivos: `interno/` (novo, com LEIA.md e mapa-sistema.json), `app/server/src/mapa.ts` (novo) + registro em `index.ts`, `app/web/src/componentes/mapa/` (novo), `app/web/src/componentes/layout/Sidebar.tsx` (item condicional), `app/web/src/componentes/layout/rotas.ts`, `app/web/src/componentes/layout/Shell.tsx` (registro da tela), `.claude/skills/atualizar/SKILL.md` (uma linha), CSS do mapa.

Prompt pronto:

> Você é o Dono C da rodada CRM v2 e Mapa do VKOS Hub. Leia primeiro os três arquivos de planos/crm-v2-e-mapa/ e siga as regras de sempre. Sua missão é a peça 3 da arquitetura: o Mapa do sistema, tela interna de visualização da arquitetura, leitura pura, que NUNCA vai pro pacote de cliente. (1) Crie interno/ na raiz com LEIA.md de uma linha ("Material interno do desenvolvimento. NUNCA entra no pacote de cliente.") e o mapa-sistema.json no formato do 02 (versao, grupos, nos com id/grupo/nome/resumo/descricao/conversaCom, ligacoes com rotulo). Conteúdo curado de 18 a 22 nós cobrindo a lista do 02; pra escrever, leia contexto/arquitetura.md e app/CONTRATO.md e DESTILE didático: resumo de uma linha de gente, descrição de 2 a 4 frases com analogia (o Barramento é o sistema nervoso, o Cérebro é a certidão de identidade do negócio, e por aí), zero tecniquês nos textos visíveis. Grupos por camada: Portas de entrada, Motor de IA, Dados do negócio, Integrações, Saída. (2) Server: app/server/src/mapa.ts com GET /api/mapa lendo interno/mapa-sistema.json resolvido a partir da raiz do repo (a pasta acima de app/; confira como o server resolve caminhos hoje); ausente ou inválido devolve { disponivel: false }, presente devolve { disponivel: true, mapa }; registre no index.ts junto das outras rotas. (3) Frontend: componentes/mapa/TelaMapa.tsx com React Flow no padrão read-only do 02 (ReactFlowProvider, fitView, hideAttribution, nodesConnectable false, nó customizado com nome + resumo + cor do grupo por token, layout determinístico em colunas por grupo calculado do JSON, clique abre painel lateral com descrição e a lista "conversa com" clicável, legenda dos grupos), CSS próprio com tokens, 3 temas. (4) Sidebar: item "Mapa" condicional que só aparece quando GET /api/mapa devolve disponivel true (padrão do item condicional de Fontes de dados, Sidebar.tsx 167 a 185); registre a tela em TELAS_FIXAS (layout/rotas.ts) e no Shell (lazy como as outras). Sem a pasta interno/, o app funciona idêntico ao de hoje e o item não existe: teste isso renomeando a pasta temporariamente. (5) Adicione uma linha no passo de levantamento de .claude/skills/atualizar/SKILL.md: conferir se interno/mapa-sistema.json reflete os módulos reais do app. (6) Confira onde a lista de exclusão do pacote de cliente está documentada (decisoes/2026-07-15-inicializador-pacote-local.md e LEIA-ME.md da raiz se existir) e reporte no relatório ONDE a menção a interno/ deve ser adicionada no fechamento (não edite decisão antiga). Ao final: npm run checar -w web e -w server limpos, relatório com a lista dos nós criados (id + nome + uma linha) e ressalvas.

## Fase 2 (após a Fase 1: dono B, consome o contrato do dono A)

### Dono B (Opus): frontend do CRM v2

Arquivos: `app/web/src/componentes/crm/` inteiro, `app/web/src/estilos/crm.css`, funções de API do CRM no client (`app/web/src/api/`, o dono confere onde as chamadas do CRM moram).

Prompt pronto:

> Você é o Dono B da rodada CRM v2 e Mapa do VKOS Hub. Leia primeiro os três arquivos de planos/crm-v2-e-mapa/ e o relatório do Dono A (o contrato das rotas novas). Siga as regras de sempre. Leia TelaCrm.tsx, ColunaCrm.tsx, CartaoContato.tsx e PainelContato.tsx inteiros antes de mexer: a mecânica de drag por pointer events, busca, mutação otimista e painel no blur é boa e deve ser PRESERVADA, você evolui o dado que ela move. Sua missão é a peça 2 da arquitetura: (1) três abas no topo da TelaCrm: Hoje, Quadro, Contatos; (2) Quadro: o kanban renderiza negócios (título + nome do contato + valor formatado), mesma mecânica de drag e colunas de hoje, mover chama a rota de mover negócio; criar negócio pergunta contato (autocomplete nos existentes ou cria novo só com o nome) + título + valor opcional; clicar abre a ficha do contato com o negócio em destaque; (3) Contatos: tabela com Nome, Empresa, Tags, Última interação, Próximo contato, Negócios (contagem e valor), busca, filtro por tag e por coluna de negócio, ordenação por cabeçalho (nome, última interação, valor); (4) Ficha do contato: evolução do PainelContato com dados no blur, bloco de negócios (criar, editar valor e título, excluir), linha do tempo de interações (composer com os 5 tipos, lista mais nova no topo, data formatada), tarefas (adicionar com prazo opcional, marcar feita com risco, excluir), próximo contato e tags como hoje, excluir contato com confirmação dupla avisando que os negócios vão junto; (5) Hoje: os quatro blocos do 02 (Follow-ups com atrasados em destaque, Esquecidos 30+ dias, Funil com contagem/valor/barrinha proporcional por token, Tarefas abertas por prazo), tudo clicável levando pra ficha ou pro quadro, número grande e rótulo claro, nada de gráfico complexo; (6) API client: funções pros endpoints novos conforme o contrato do A; (7) crm.css evoluído com tokens, reaproveitando classes existentes onde der, 3 temas, motion sutil. Estados vazios com mensagem curta e gentil (CRM novo sem nada mostra a aba Hoje com o convite de criar o primeiro contato). Ao final: npm run checar -w web limpo e relatório com o que mudou por aba e ressalvas.

## Fase 3: QA de gesto real (Opus)

Prompt pronto:

> Você é o QA da rodada CRM v2 e Mapa do VKOS Hub. Leia os três arquivos de planos/crm-v2-e-mapa/ e os relatórios dos donos. Playwright real em http://localhost:5173 (ou build na 4600), viewport 1440x900, screenshot de cada passo. Regras da casa: sessões de IA reais custam dinheiro; você tem direito a UMA sessão real, no passo 6. IMPORTANTE: os dados reais do CRM do Jesse são sagrados; faça os gestos destrutivos num workspace de teste (crie um workspace novo pela UI ou use o Estúdio Aura se o executor confirmar que é de teste; NUNCA no OJESSEGOMES). Roteiro: (1) migração: confira por leitura dos backups do Dono A e do crm.json migrado do workspace do Jesse (SEM editar nada) que cada cartão v1 virou ficha + negócio com tudo preservado; liste 3 contatos de amostra comparando antes e depois campo a campo; (2) CRM ponta a ponta no workspace de teste: criar contato, criar negócio pra ele, mover no quadro entre 3 colunas, registrar interação de cada tipo, criar tarefa com prazo e marcar feita, definir próximo contato e conferir que o evento aparece no Calendário local (sincronizarCrm ligado), editar valor do negócio e ver o total da coluna mudar, buscar e filtrar na aba Contatos, ordenar por valor, conferir a aba Hoje refletindo follow-up e tarefas criados; (3) compatibilidade: criar uma automação "quando contato mudar de coluna" pela tela Automações no workspace de teste, mover um negócio e conferir no histórico/ensaio que a regra reconheceu o evento com as variáveis certas; (4) Mapa: item na sidebar presente, tela abre com a rede completa, clique em 3 nós mostra painel didático, legenda ok; renomear interno/ temporariamente pra interno-off/ e conferir que o item some da sidebar e o app segue normal (restaurar depois); conferir os 3 temas no Mapa e nas 3 abas do CRM (screenshots); (5) grep de conformidade: nenhum texto novo com travessão ou ponto centrado; nenhuma cor hardcoded nos arquivos novos (procurar #hex fora de global.css e do mapa-sistema.json); (6) A SESSÃO REAL: no workspace de teste com 3 ou 4 contatos e interações que descrevam dores claras (ex: "cliente reclamou que orçamento demora", "achou caro mas fechou pelo prazo"), dispare pelo cockpit uma sessão com o pedido "Me diga, com base no meu CRM, quais as 3 principais dores dos meus clientes e como usar isso num carrossel" e confira: a resposta usa o contexto injetado (cita as dores das interações), NÃO expõe telefone/email, e a transcrição mostra que o resumo chegou; (7) resíduos: apagar o que o roteiro criou no workspace de teste, confirmando antes com o executor. Relatório: matriz passo a passo com veredito, bugs com severidade e arquivo:linha, screenshots. Não conserte nada: só reporte.

## Checklist de fechamento (executor)

1. Typecheck web e server limpos, testes do server verdes.
2. Bugs do QA de severidade média pra cima: corrigir pelo dono da fronteira certa e revalidar o gesto afetado.
3. `npm run build -w web` a partir de `app/` e conferir que a 4600 serve o hash novo.
4. Atualizar `app/CONTRATO.md`: modelo v2 do CRM (contatos, negócios, interações, tarefas, migração), rotas novas, eventos (payload do movido com negocio, o interacao-registrada novo), o resumo do CRM e a injeção por menção no prompt com a regra anti-vazamento, a rota GET /api/mapa e o comportamento condicional da tela Mapa.
5. Atualizar `contexto/arquitetura.md`: a linha do CRM na seção da rodada 10/11 e uma menção curta na seção de contexto de sessão (o CRM injeta resumo quando citado). Atualizar `contexto/roadmap.md` na Fase 6 (CRM) com a entrega v2.
6. Adicionar a menção de `interno/` na lista de exclusão do pacote onde o Dono C indicou no relatório dele.
7. Atualizar `interno/mapa-sistema.json` se a própria rodada tiver mudado a arquitetura (o CRM v2 muda o nó do CRM: descrição menciona negócios e o contexto pra IA).
8. Registrar decisões curtas em `decisoes/`: (a) CRM v2 com negócios separados de contatos, migração sem perda, eventos compatíveis; (b) contexto do CRM injetado por menção no prompt com regra anti-vazamento, dado pessoal nunca em peça; (c) Mapa do sistema como tela interna com dados em interno/, fora do pacote de cliente por construção, sem flag de build.
9. Avisar o Jesse com o resumo honesto e as ressalvas.
10. Perguntar ao Jesse se pode apagar a pasta `planos/crm-v2-e-mapa/`.
